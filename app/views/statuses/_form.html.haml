= simple_form_for @status, :html => {:class => 'form-horizontal well'} do |f|
	= f.input :name
	= f.input :description

	.form_actions
		= f.button :submit
		= link_to t('helpers.submit.cancel').html_safe, statuses_path, :class => 'btn'

